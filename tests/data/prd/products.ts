import type { ProductRow } from "../types";

/** Production: a single canary item — minimize side effects against live. */
export const products: ProductRow[] = [
  { slug: "sauce-labs-backpack", name: "Sauce Labs Backpack" },
];
